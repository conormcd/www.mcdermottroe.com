checkout:
  post:
    - git submodule update --init

dependencies:
  pre:
    - mkdir -p ~/php-ext
    - pear config-set ext_dir ~/php-ext
    - yes no | pecl install --alldeps apc
    - pecl install xdebug
    - pear channel-discover pear.phpmd.org
    - pear install --alldeps phpmd/PHP_PMD
