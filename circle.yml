machine:
  php:
    version: 5.4.37

dependencies:
  override:
    - sed -i 's/^;//' ~/.phpenv/versions/$(phpenv global)/etc/conf.d/xdebug.ini
    - pecl install -f memcached-2.0.1
    - make vendor

test:
  override:
    - make
